descriptor_version: 1
name: biokg
provider: stub
provider_version: "1"
description: Private BioKG-Align release binding; awaiting the user-confirmed local build/release package.
upstream:
  local_release_subdirectory: BioKG-Align/build/release
  availability: awaiting_local_release
  binding_help: Bind the completed local release after the user announces it; no public download is assumed.
tasks:
  csv-kg:
    source: source
    target: target
    refs: {test: references/test.tsv}
    candidates: candidates/test.cands.tsv
    extras: {layout: csv_kg, unpublished: true}
