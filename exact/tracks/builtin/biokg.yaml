descriptor_version: 1
name: biokg
provider: stub
provider_version: "1"
description: Version-pinned local BioKG CSV bindings are available; automatic provider materialization is not implemented.
upstream:
  local_release_subdirectory: BioKG-Align/build/release
  availability: explicit_local_bindings
  binding_help: Use hashed CaseBindings for the selected public release and io.input_format=csv-kg; train/valid labels and reference-free submission inputs are prepared separately.
tasks:
  csv-kg:
    source: source
    target: target
    refs: {test: references/test.tsv}
    candidates: candidates/test.cands.tsv
    extras: {layout: csv_kg, automatic_materialization: false}
