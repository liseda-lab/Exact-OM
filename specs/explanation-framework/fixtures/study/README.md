# Synthetic study seed fixtures

All examples are synthetic, with no real participant, issued token or hosted study. Validate against `../../protocol/study.schema.json`. Private case kind/acceptable targets are deliberately absent; scoring and allocation examples live under `protocol/` as researcher-only design artifacts. They must not be served as participant data. A passing schema/semantic check does not establish working invitations, transactional persistence or Render deployment; B0/B3/B5 must implement and verify those.
