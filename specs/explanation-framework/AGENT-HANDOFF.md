# Backend implementation assignment; frontend deferred

Implement B0–B5 from this suite in the existing Exact-OM repository. The user has chosen a separate backend review and a specialized frontend agent **only after backend readiness**, for full frontend design/implementation. Do not create an early frontend review dependency or implement a speculative full frontend while building the backend.

Read README, status, contracts, 02–07 and 09–13; inspect the current checkout and preserve unrelated changes. The inventory is a baseline, not a claim that missing features are now implemented. Reuse pyowl-core/Exact storage and producers; no second matcher/parser/workflow platform. All generative calls go through OpenRouter, using existing authorized configuration and recorded costs. The user has broad spending discretion; do not block preparation on inventing another arbitrary monetary cap. Concurrency/retry bounds still apply.

1. Resolve supported installed packages, pinned NCIT–DOID bytes/imports and node profile. Fix stale data descriptors. Freeze target contracts/policies and materialize stage-scoped execution locks. Bind the fresh run before run-import and each OpenRouter profile before its own dispatch; future bindings must not block independent indexing.
2. Build independent context and minimal Exact exports in parallel where safe. Prove one ontology-only query and one real imported pair before generalizing.
3. Add lazy bounded APIs, policy-filtered fact packets, prepared independent profiles/comparisons and validated local bundle import. Enforce fixed-demo and study deployment boundaries. Implement anonymous reusable invitations, durable assignment/forms/rankings/consultation/events, researcher administration/exports and the PostgreSQL/Render runbook. Verify missing/provenance/error states throughout.
4. Implement dependency-aware artifact recovery and transactional participant recovery/isolation before large preparation or study deployment. Complete the 12-pair backend operational path (distinct from participant case count), bounded generation development and synthetic mixed-study workflow/metric/restart checks. Optional final case-count choice and recruitment do not block implementation.
5. Pass 07, publish the backend handoff with evidence and exact continuation commands. Update the status ledger honestly and list optional deferred capabilities.
6. Stop this assignment at backend handoff unless explicitly assigned F1 as well. The later specialized agent receives 08 and the working package; it performs full frontend design and implementation for the exploration app and complete study then. Participant public launch is a later readiness step, not implied by backend handoff.

Do not silently change matching decisions, treat default relation confidence as certainty, infer original axioms from labels, generate missing biomedical facts, or present fixture/model/participant results that were not produced. Optional inference/KGA/repair cannot become hidden prerequisites for the class-pair handoff. If a required input genuinely cannot be resolved, report the exact missing binding and continue independent implementation.

This specification-writing task does not itself authorize automatic launch of the full matching experiment campaign or participant recruitment. A subsequent implementation assignment follows its stated operational scope; the existing separately authorized experiment campaign retains its own rules.
